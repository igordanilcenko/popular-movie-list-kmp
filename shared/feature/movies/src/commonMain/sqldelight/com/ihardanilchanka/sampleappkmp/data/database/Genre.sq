CREATE TABLE GenreEntity (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL
);

getAll:
SELECT * FROM GenreEntity;

insertGenre:
INSERT OR REPLACE INTO GenreEntity(id, name) VALUES (?, ?);

deleteAll:
DELETE FROM GenreEntity;
